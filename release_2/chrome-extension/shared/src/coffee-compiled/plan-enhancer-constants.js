// Generated by CoffeeScript 2.7.0
(function() {
  /*
   * PlanEnhancer constants, helpers, and SimpleCache.
   * Extracted from plan-enhancer.coffee to keep the main module lean.
   */
  var DEFAULTS, DEFAULT_FOCUS_PROMPTS, DEFAULT_MESSAGES, EFFICIENCY_WEIGHTS, MS_PER_DAY, MUSIC_GENRES, MUSIC_PRESETS, MUSIC_SLASH_FLAGS, MUSIC_SOURCE_TYPES, PRESETS, SimpleCache, TIP_THRESHOLDS, WASM_TARGETS, deepFreeze, isFiniteNumber, isNonNegativeInt, isPlainObject, isPositiveInt,
    hasProp = {}.hasOwnProperty;

  // ── Helpers ------------------------------------------------------------------
  isFiniteNumber = function(v) {
    return typeof v === 'number' && Number.isFinite(v);
  };

  isPositiveInt = function(v) {
    return isFiniteNumber(v) && Number.isInteger(v) && v > 0;
  };

  isNonNegativeInt = function(v) {
    return isFiniteNumber(v) && Number.isInteger(v) && v >= 0;
  };

  isPlainObject = function(v) {
    var proto;
    if (!((v != null) && typeof v === 'object')) {
      return false;
    }
    proto = Object.getPrototypeOf(v);
    return proto === null || proto === Object.prototype;
  };

  // Recursively freezes an object and all nested objects/arrays.
  // Unlike Object.freeze (shallow), this guarantees deep immutability.
  // Children are frozen before their parent (bottom-up) so a failure
  // mid-tree doesn't leave a partially-frozen outer shell.
  // Circular references are detected and skipped via the `seen` Set
  // to prevent infinite recursion.
  deepFreeze = function(obj, seen) {
    var key, val;
    if (seen == null) {
      seen = new Set();
    }
    if (!((obj != null) && typeof obj === 'object')) {
      return obj;
    }
    if (Object.isFrozen(obj) || seen.has(obj)) {
      return obj;
    }
    seen.add(obj);
    for (key in obj) {
      if (!hasProp.call(obj, key)) continue;
      val = obj[key];
      if ((val != null) && typeof val === 'object') {
        deepFreeze(val, seen);
      }
    }
    Object.freeze(obj);
    return obj;
  };

  /*
   * Lightweight LRU cache with per-entry TTL.
   * Doubly-linked list + Map — zero external dependencies.
   *
   *   • set / get promote entries to most-recent position (LRU eviction)
   *   • peek reads without promotion or stats side-effects
   *   • expired entries are lazily evicted on access;
   *     call purgeExpired() to proactively remove stale items
   *
   * Note: set() evicts the LRU tail when full without scanning for expired
   * entries first (that would make set O(n)). Call purgeExpired() before
   * critical inserts if you need to reclaim stale slots proactively.
   */
  SimpleCache = class SimpleCache {
    constructor(maxSize = 500, defaultTtlMs = 300000) {
      this.maxSize = isPositiveInt(maxSize) ? maxSize : 500;
      this.defaultTtlMs = isPositiveInt(defaultTtlMs) ? defaultTtlMs : 300000;
      this.cache = new Map(); // Maps keys to cache nodes for O(1) lookups
      this.head = null; // Most recently used node
      this.tail = null; // Least recently used node
      this.hitCount = 0; // Total cache hits for metrics
      this.missCount = 0; // Total cache misses for metrics
    }

    
      // ── Doubly-linked list primitives ─────────────────────────────────────────
    _removeNode(node) {
      var next, prev;
      // Safely unlinks a node from the linked list, updating head/tail
      ({prev, next} = node);
      if (prev) {
        prev.next = next;
      } else {
        this.head = next;
      }
      if (next) {
        next.prev = prev;
      } else {
        this.tail = prev;
      }
      node.prev = node.next = null;
      return node;
    }

    _addToHead(node) {
      // Adds a node to the front (MRU position) of the linked list
      node.next = this.head;
      node.prev = null;
      if (this.head) {
        this.head.prev = node;
      }
      this.head = node;
      if (!this.tail) {
        this.tail = node;
      }
      return node;
    }

    _removeTail() {
      var oldTail;
      if (!this.tail) {
        // Removes the LRU node from the tail and deletes it from the cache
        return null;
      }
      oldTail = this.tail;
      this.cache.delete(oldTail.key);
      this._removeNode(oldTail);
      return oldTail;
    }

    _promoteToMRU(node) {
      if (node === this.head) {
        // Moves an accessed node to the MRU position to maintain LRU order
        return node;
      }
      this._removeNode(node);
      return this.addToHead(node);
    }

    _evictExpiredNode(node, key) {
      // Internal helper to clean up an expired node
      this.cache.delete(key);
      return this._removeNode(node);
    }

    // ── Public API ────────────────────────────────────────────────────────────
    set(key, value, ttlMs) {
      var existingNode, newNode, now, ttl;
      ttl = isPositiveInt(ttlMs) ? ttlMs : this.defaultTtlMs;
      now = Date.now();
      existingNode = this.cache.get(key);
      if (existingNode) {
        // Update existing entry's value and expiration, then refresh LRU position
        existingNode.value = value;
        existingNode.expiresAt = now + ttl;
        this._promoteToMRU(existingNode);
      } else {
        if (this.cache.size >= this.maxSize) {
          // Evict LRU entry if cache is at capacity before adding new entry
          this._removeTail();
        }
        newNode = {
          key,
          value,
          expiresAt: now + ttl,
          prev: null,
          next: null
        };
        this.cache.set(key, newNode);
        this._addToHead(newNode);
      }
      return true;
    }

    get(key) {
      var node;
      // Retrieves a value, updates LRU order and hit/miss statistics
      node = this.cache.get(key);
      if (!node) {
        this.missCount++;
        return null;
      }
      if (Date.now() > node.expiresAt) {
        this._evictExpiredNode(node, key);
        this.missCount++;
        return null;
      }
      this.hitCount++;
      this._promoteToMRU(node);
      return node.value;
    }

    peek(key) {
      var node;
      // Read-only access that doesn't modify LRU order or statistics
      node = this.cache.get(key);
      if (!node) {
        return null;
      }
      if (Date.now() > node.expiresAt) {
        this._evictExpiredNode(node, key);
        return null;
      }
      return node.value;
    }

    has(key) {
      var node;
      // Checks for existence of a valid (non-expired) entry
      node = this.cache.get(key);
      if (!node) {
        return false;
      }
      if (Date.now() > node.expiresAt) {
        this._evictExpiredNode(node, key);
        return false;
      }
      return true;
    }

    delete(key) {
      var node;
      // Removes a specific entry from the cache
      node = this.cache.get(key);
      if (!node) {
        return false;
      }
      this.cache.delete(key);
      this._removeNode(node);
      return true;
    }

    purgeExpired() {
      var now;
      // Removes all expired entries from the cache to free up memory
      now = Date.now();
      this.cache.forEach((node, key) => {
        if (now > node.expiresAt) {
          return this._evictExpiredNode(node, key);
        }
      });
      return this;
    }

    clear() {
      // Wipes the entire cache and resets all state and statistics
      this.cache.clear();
      this.head = this.tail = null;
      this.hitCount = this.missCount = 0;
      return this;
    }

    size() {
      return this.cache.size; // Returns current number of valid entries in cache
    }

    forEach(fn) {
      var currentNode, nextNode, results;
      // Iterates entries from most-recent to least-recent. Does not evict expired entries.
      // Call purgeExpired() first if you only want to process active entries.
      // Safely handles mutations during iteration by pre-capturing next pointers.
      currentNode = this.head;
      results = [];
      while (currentNode) {
        nextNode = currentNode.next;
        fn(currentNode.value, currentNode.key);
        results.push(currentNode = nextNode);
      }
      return results;
    }

    entries() {
      var currentNode, result;
      // Returns array of [key, value] pairs ordered MRU → LRU
      result = [];
      currentNode = this.head;
      while (currentNode) {
        result.push([currentNode.key, currentNode.value]);
        currentNode = currentNode.next;
      }
      return result;
    }

    keys() {
      var currentNode, result;
      // Returns array of all cache keys ordered MRU → LRU
      result = [];
      currentNode = this.head;
      while (currentNode) {
        result.push(currentNode.key);
        currentNode = currentNode.next;
      }
      return result;
    }

    values() {
      var currentNode, result;
      // Returns array of all cache values ordered MRU → LRU
      result = [];
      currentNode = this.head;
      while (currentNode) {
        result.push(currentNode.value);
        currentNode = currentNode.next;
      }
      return result;
    }

    getStats() {
      var hitRate, totalRequests;
      // Returns performance metrics including hit rate for monitoring
      totalRequests = this.hitCount + this.missCount;
      hitRate = totalRequests === 0 ? 0 : this.hitCount / totalRequests;
      return {
        size: this.cache.size,
        hits: this.hitCount,
        misses: this.missCount,
        hitRate
      };
    }

    resetStats() {
      // Resets hit/miss counters to zero
      this.hitCount = this.missCount = 0;
      return this;
    }

  };

  // ── Default i18n messages ────────────────────────────────────────────────────
  DEFAULT_MESSAGES = deepFreeze({
    taskPartFmt: function(title, idx) {
      return `${title} - Part ${idx + 1}`;
    },
    microStepFmt: function(title, idx) {
      return `${title} - Step ${idx + 1}`;
    },
    presets: {
      micro: 'Micro session',
      pomodoro: 'Pomodoro',
      sprint: 'Sprint mode',
      deep_work: 'Deep work'
    },
    tips: {
      struggling: 'Consider shortening your focus chunks — 20–25 minutes may better match your attention span.',
      excelling: 'Great progress! Try extending your focus blocks slightly for deeper work.'
    },
    transitionChecklist: {
      saveWork: 'Save current work',
      closeTabs: 'Close unnecessary tabs',
      gatherMaterials: 'Gather materials for next task',
      shortBreak: 'Take a 30-second break'
    },
    transitionNameFmt: function(from, to) {
      return `Transition: ${from} → ${to}`;
    },
    motivationalQuotes: {
      low: ['Every expert was once a beginner. One small step is enough.', 'Progress beats perfection. Start with the easiest part.', 'Slow and steady wins the race.'],
      medium: ['You are making progress. Keep going.', 'Consistency builds momentum.', 'Great work — keep the momentum alive.'],
      high: ['Outstanding! You are in the zone.', 'Fantastic rhythm — keep the streak alive!', 'Top-tier performance. Excellent work!']
    }
  });

  // ── Scheduling constants ─────────────────────────────────────────────────────
  DEFAULTS = Object.freeze({
    chunkSize: 25,
    breakDuration: 5
  });

  WASM_TARGETS = Object.freeze({
    focus: 20,
    break: 70
  });

  EFFICIENCY_WEIGHTS = Object.freeze({
    completion: 0.7,
    timeliness: 0.3
  });

  TIP_THRESHOLDS = Object.freeze({
    struggling: 0.5,
    excelling: 0.9
  });

  // FIX: PRESETS contains nested objects — Object.freeze only freezes the array
  // shell, leaving each preset object mutable. deepFreeze freezes recursively.
  PRESETS = deepFreeze([
    {
      name: 'micro',
      duration: 15,
      break: 3,
      description: null,
      maxAvg: 20
    },
    {
      name: 'pomodoro',
      duration: 25,
      break: 5,
      description: null,
      maxAvg: 45
    },
    {
      name: 'sprint',
      duration: 50,
      break: 10,
      description: null,
      maxAvg: 80
    },
    {
      name: 'deep_work',
      duration: 90,
      break: 20,
      description: null,
      maxAvg: 2e308
    }
  ]);

  DEFAULT_FOCUS_PROMPTS = Object.freeze(['Take a 10-second pause and notice your breath.', 'What is one tiny step you can take right now?', 'Remember the goal of this session.', 'Take one deep breath and gently refocus.', 'Silently name three things that can wait until later.']);

  MS_PER_DAY = 86400000; // Milliseconds in one full day (24 * 60 * 60 * 1000)

  
  // ── Music constants (mirrors PHP PlanParser + MusicService) ──────────────────
  MUSIC_PRESETS = deepFreeze([
    {
      id: 'lofi-focus',
      title: 'Lo-fi Focus Session',
      duration: 90,
      goal: 'Ambient focus',
      musicPreset: 'lofi',
      genre: 'ambient',
      source: 'all'
    },
    {
      id: 'classical-study',
      title: 'Classical Study Session',
      duration: 60,
      goal: 'Deep study',
      musicPreset: 'classical',
      genre: 'classical',
      source: 'all'
    },
    {
      id: 'white-noise',
      title: 'White Noise Session',
      duration: 120,
      goal: 'Noise isolation',
      musicPreset: 'noise',
      genre: 'noise',
      source: 'local'
    },
    {
      id: 'binaural',
      title: 'Binaural Focus Session',
      duration: 45,
      goal: 'Binaural focus',
      musicPreset: 'binaural',
      genre: 'binaural',
      source: 'local'
    },
    {
      id: 'ambient-code',
      title: 'Ambient Coding Session',
      duration: 120,
      goal: 'Flow coding',
      musicPreset: 'ambient',
      genre: 'electronic',
      source: 'all'
    },
    {
      id: 'energize',
      title: 'Energize Sprint',
      duration: 25,
      goal: 'Upbeat energy',
      musicPreset: 'upbeat',
      genre: 'electronic',
      source: 'all'
    }
  ]);

  MUSIC_GENRES = Object.freeze(['ambient', 'classical', 'noise', 'binaural', 'electronic', 'lofi', 'jazz', 'instrumental', 'soundtrack', 'blues', 'folk', 'rock', 'pop']);

  MUSIC_SOURCE_TYPES = Object.freeze(['local', 'youtube', 'spotify', 'soundcloud', 'all']);

  MUSIC_SLASH_FLAGS = deepFreeze([
    {
      flag: '--music',
      valueHint: '<preset>',
      values: MUSIC_PRESETS.map(function(p) {
        return p.id;
      }),
      description: 'Apply a music focus preset'
    },
    {
      flag: '--playlist',
      valueHint: '<playlistId>',
      values: null,
      description: 'Attach a playlist by ID'
    },
    {
      flag: '--source',
      valueHint: '<source>',
      values: MUSIC_SOURCE_TYPES.slice(0,
    -1),
      description: 'Restrict sources'
    },
    {
      flag: '--genre',
      valueHint: '<genre>',
      values: MUSIC_GENRES.slice(),
      description: 'Filter by genre'
    }
  ]);

  // ── Exports ──────────────────────────────────────────────────────────────────
  module.exports = {
    // Core classes
    SimpleCache: SimpleCache,
    // Shared constants
    DEFAULT_MESSAGES: DEFAULT_MESSAGES,
    DEFAULTS: DEFAULTS,
    WASM_TARGETS: WASM_TARGETS,
    EFFICIENCY_WEIGHTS: EFFICIENCY_WEIGHTS,
    TIP_THRESHOLDS: TIP_THRESHOLDS,
    PRESETS: PRESETS,
    DEFAULT_FOCUS_PROMPTS: DEFAULT_FOCUS_PROMPTS,
    MS_PER_DAY: MS_PER_DAY,
    // Music-related constants
    MUSIC_PRESETS: MUSIC_PRESETS,
    MUSIC_GENRES: MUSIC_GENRES,
    MUSIC_SOURCE_TYPES: MUSIC_SOURCE_TYPES,
    MUSIC_SLASH_FLAGS: MUSIC_SLASH_FLAGS,
    // Internal utilities (exposed for testing/consumption)
    isFiniteNumber: isFiniteNumber,
    isPositiveInt: isPositiveInt,
    isNonNegativeInt: isNonNegativeInt,
    isPlainObject: isPlainObject,
    deepFreeze: deepFreeze
  };

}).call(this);
